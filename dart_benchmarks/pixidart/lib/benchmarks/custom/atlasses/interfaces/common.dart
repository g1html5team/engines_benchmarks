import 'package:pixi_dart_benchmark/interfaces/common.dart';

abstract class AtlasInterface extends Interface {
  removeChildren(covariant object);
  setBitmapData(covariant bitmap, covariant data);
}

import 'package:pixi_dart_benchmark/interfaces/common.dart';

abstract class VisibilityInterface extends Interface {
  setVisible(covariant child, bool visibility);

  setScale(covariant object, num value);
}

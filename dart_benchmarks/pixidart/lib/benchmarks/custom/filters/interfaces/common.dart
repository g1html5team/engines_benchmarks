import 'package:pixi_dart_benchmark/interfaces/common.dart';

abstract class FilterInterface extends Interface {
  setBrightness(covariant object, num value);
  setHue(covariant object, num value);
}
